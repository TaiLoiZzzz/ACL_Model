server:
  port: 8081

spring:
  application:
    name: backend-service
  profiles:
    active: @spring.profiles.active@

management:
  endpoints:
    web:
      exposure:
        include: '*'
  health:
    mail:
      enabled: false

logging:
  level:
    root: INFO
    web: INFO




